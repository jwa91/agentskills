target-version = "py311"
line-length = 88
src = ["src"]

exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".hg",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "venv",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "**/__pycache__",
    "*.pyc",
]

[lint]
select = [
    "E",       # pycodestyle errors
    "W",       # pycodestyle warnings
    "F",       # pyflakes
    "I",       # isort
    "UP",      # pyupgrade
    "B",       # flake8-bugbear
    "N",       # pep8-naming
    "C4",      # flake8-comprehensions
    "SIM",     # flake8-simplify
    "PLC0415", # import-outside-toplevel
    "G",       # flake8-logging-format
    "D",       # pydocstyle
]

ignore = ["N805", "D401", "D105"]

[lint.pydocstyle]
convention = "google"

[lint.per-file-ignores]
"tests/**" = ["D"]
"src/agentskills/cli.py" = ["PLC0415"]

# Skill scripts use inline HTML/JS templates that produce unavoidable long lines.
# Renderer functions are internal (dispatched via RENDERERS dict) and don't need docstrings.
"skills/**/scripts/build-lesson.py" = ["E501", "D103", "PLC0415"]
"skills/**/scripts/build-dashboard.py" = ["E501", "D103", "B905", "PLC0415"]
"skills/**/scripts/find-videos.py" = ["E501", "D103", "SIM105"]
"skills/**/scripts/progress.py" = ["E501", "D103"]

[format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
